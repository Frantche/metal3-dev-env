{
    "insecure-registries" : ["{{ REGISTRY }}"],
    "proxies": {
        "http-proxy": "{{ HTTP_PROXY }}",
        "https-proxy": "{{ HTTPS_PROXY }}",
        "no-proxy": "{{ NO_PROXY_CLEANED }}"
      }
}
